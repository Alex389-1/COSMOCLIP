# ==============================================================================
# SatQuery AI — Frontend Environment Configuration (.env)
# ==============================================================================

# Backend API proxy or base URL (Defaults to Vite proxy at /api)
VITE_API_BASE_URL=http://localhost:8000

# Optional Map tile provider token (if using custom Mapbox/Esri layers)
# VITE_MAPBOX_ACCESS_TOKEN=
